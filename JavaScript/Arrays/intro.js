// Arrays, unlike objects, are ORDERED
// They are zero based number indexed
// They are nonhomogeneous
// constructed with square brackets
// All have length property

const ARRAY = ['bill', 2, [0], { name: 'ted' }]

const OBJECT = {
  name: 'I am an object',
}

const ARRAY2 = [1, 2, 3]
